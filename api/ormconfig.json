{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "docker",
  "password": "docker",
  "database": "nest-api",
  "entities": ["src/db/models/*.entity.ts"],
  "migrations": ["src/db/migrations/*.ts"],
  "cli": {
    "migrationsDir": ["src/db/migrations"]
  }
}
